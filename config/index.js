const config = {
    database: '',  // путь к базе данных
    port: 3000,   // порт на котором сервер слушает подключение
    secret: 'secretKey'  // секретный ключ для сесий
};

export default config;